import { getStandalonePageMetadata } from '@/lib/seo/standalonePages'

export const metadata = getStandalonePageMetadata('/premium/success')

export default function PageLayout({ children }) {
  return children
}
